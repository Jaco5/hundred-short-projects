# functions with more than one parameter
# when functions have more htan one parameter, arguments are positional or given with keyword. 

def myfunction(a, b, c):
    if a + b = c:
        return 1
    else:
        return 2

myfunction(1, 2, 3)
#or
myfunction(b=2, a=1, c=3)
#this garauntees values go to correct places

